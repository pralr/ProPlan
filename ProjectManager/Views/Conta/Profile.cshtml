@using Microsoft.AspNetCore.Identity
@model UsuarioViewModel

@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    var userId = UserManager.GetUserId(User);
    ViewData["Id"] = userId;
} 


<!------ TODO: Criar um alterações salvas com sucesso--->

<script src="//netdna.bootstrapcdn.com/bootstrap/3.1.0/js/bootstrap.min.js"></script>
<script src="//code.jquery.com/jquery-1.11.1.min.js"></script>
<!------ Include the above in your HEAD tag ---------->

<br/><br/>
<div class="container d-flex justify-content-center align-items-center">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-6 col-lg-12 col-xs-offset-0 col-sm-offset-0 col-md-offset-3 col-lg-offset-3 toppad">

            <!--Para envivar a view model do form, basta add o metodo "post"-->
            <form asp-action="Edit" asp-route-id="@ViewData["Id"]" method="post"> 
            <div class="panel panel-info">
                    <div class="panel-heading">
                    <h3 class="panel-title">@Model.Nome @Model.Sobrenome</h3>
                </div>
                <div class="panel-body">
                    <div class="row">
                        
                        <div class=" col-md-9 col-lg-12 ">
                            <table class="table table-user-information">
                                <tbody>
                                   @*  <tr>
                                        <td>
                                            <div class="col-md-3 col-lg-3 " align="center">
                                                <img alt="User Pic" src="https://static.vecteezy.com/ti/vetor-gratis/p1/2318271-icone-do-perfil-do-usuario-vetor.jpg" class="img-circle img-responsive" style="width: 150px; height: 150px;">
                                            </div>
                                        </td>
                                    </tr> *@
                                     <tr>
                                        <tr>
                                            <td><label asp-for="Descricao" class="control-label"></label></td>
                                            <td><input readonly asp-for="Descricao" class="form-control" style="width: 100%; height:50px; text-align: left;" /></td>
                                        </tr>
                                        <tr>
                                            <td><label asp-for="TipoCargo" class="control-label"></label></td>
                                            <td>
                                                <select readonly asp-for="TipoCargo" class="form-control" asp-items="ViewBag.TipoCargo as IEnumerable<SelectListItem>" style="width: 100%;"></select>
                                            </td>
                                        </tr>


                                    <tr>
                                        <td><label asp-for="Email" class="control-label"></label></td>
                                            <td><input readonly asp-for="Email" class="form-control" /></td>
                                    </tr>
                                    <tr>
                                        <td><label asp-for="Telefone" class="control-label"></label></td>
                                            <td><input readonly asp-for="Telefone" class="form-control" /></td>
                                    </tr>              
                                </tbody>
                            </table>

                                <div class="form-group">
                                    <a asp-controller="Usuario" asp-action="Index" class="btn btn-primary">Voltar aos usuários</a>
                                </div>
                        </div>
                    </div>
                </div>
            </div>
            </form>
        </div>
    </div>
</div>
